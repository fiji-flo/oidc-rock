apiVersion: v1
kind: Service
metadata:
  name: {{ include "oidc-rock.fullname" . }}
  labels:
    {{- include "oidc-rock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oidc-rock.selectorLabels" . | nindent 4 }}
